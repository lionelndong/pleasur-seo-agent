# Deployed VPS source snapshot

This directory is a read-only source snapshot copied directly from the live Paperclip VPS on 2026-07-25. It is included so the routine, skills, references, templates, examples, and scripts can be inspected exactly as deployed.

Included source directories are `.claude`, `references`, `templates`, `docs`, `examples`, and `scripts`, plus the root operating documents `PIPELINE.md`, `CLAUDE.md`, `README.md`, and `brand-config.md`.

Runtime secrets, credentials, environment files, and generated operational data are deliberately not published. The broader historical source package, including its content-pipeline materials and run artifacts, is in `../exact-blog-agent-2/`.

For the current deployment fingerprints and complete deployed skill inventory, see `../live-routines/VPS-VERIFICATION.md`.
