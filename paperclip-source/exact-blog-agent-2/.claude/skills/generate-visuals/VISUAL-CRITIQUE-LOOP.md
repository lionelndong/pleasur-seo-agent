# Visual critique loop — LOCKED 2026-06-28 (render → critique → fix → re-render → gate)

**Every generated visual (annotation, infographic, chart, card, cover, action-shot) MUST pass this loop before it is
published.** AI mistakes — wrong target, garbled/clipped text, covered content, off-brand —
get caught here, not by readers. Modeled on the infographic blueprint-critique loop.

## Loop (max 3 iterations)
1. **Render** the visual.
2. **Deterministic checks** (machine, no AI):
   - *annotation* — read the `<out>_report.json`: is every target `found:true`? any `edge_clipped:true`?
     Always run `annotate_screenshot.py --strict` so a missing target **hard-fails** (NO silent skip — mandate).
   - *infographic / chart / card / cover* — file exists, non-empty, expected dimensions (cover = exactly **1600×900**); engine printed `status: captured`.
   - *action-shot* — read the `<out>_report.json`: `status` is captured (NOT `session_required` /
     `cloudflare_challenge_unresolved`); for an authed preset `auth_used:true` and no `logged_out`;
     `raw_dims` present and retina (~2880 wide desktop / ~860 wide phone).
   - *concept illustration* — `python concept_palette_check.py --in raw.png` catches palette drift
     (e.g. a cyan/teal hero instead of brand blue→violet); `WARN` ⇒ scrutinize hard. File exists,
     non-empty, expected aspect.
3. **Vision critique** — the agent looks at the rendered PNG against the checklist below and
   returns JSON `{pass: bool, issues: [{idx, problem, fix}]}`. Be a STRICT critic: if unsure, fail it.
4. If not `pass` → **apply the fixes** to the spec and go to 1:
   - annotation: change `selector` / `kind` / `corner` / `pad` / `color`.
   - infographic: fix `content` / wording / layout / item count.
   - chart: fix data / labels / palette.
   - card: fix the spec — value / label / delta+tone / quote / variant / icon / width.
   - cover: fix `theme` / `accent` / `icon` / title length / eyebrow; (route B) re-roll the metaphor or fall back to route A.
5. After **3 failed passes** → **DO NOT publish.** Flag a human with the issues + the latest render.

## Checklists
### Annotation
- [ ] Each annotation sits on the **correct** element (matches its note/intent).
- [ ] All target text fully visible — **no clipping** (especially zoom insets), no cut words.
- [ ] No annotation/inset **covers other important content** (prices, CTAs, other annotations).
- [ ] Labels legible, not overlapping each other or key UI.
- [ ] Highlight fill keeps the underlying text readable.
- [ ] Colors on-brand (blue `#2E90FA` default), consistent; numbering reads top→bottom / left→right.

### Infographic
- [ ] Every word + number spelled correctly and **fully inside the frame** (no crop).
- [ ] Text is clean sans (NOT handwritten); only the illustration is hand-drawn.
- [ ] Clear hierarchy, uncluttered; on-brand palette; takeaway present.
- [ ] **Real logo** present (composited via `composite_logo.py`, never AI-drawn).

### Chart
- [ ] Axes / labels / legend correct + legible; numbers **match the source data**.
- [ ] On-brand palette + font; clean canvas (not default matplotlib); no truncated labels.

### Card (stat / quote / callout)
- [ ] Every word + number spelled right and **fully inside the card** (no clipped value, label, or quote line).
- [ ] Stat numbers **match the source**; the delta arrow/colour points the right way (`tone` set when "down" is good).
- [ ] Quote is verbatim; attribution name/role correct; avatar initials match the name.
- [ ] Callout `variant` fits the message (warning ≠ tip); icon + label + accent are consistent.
- [ ] On-brand: palette only, IBM Plex/Geist, real logo bottom-right, white card on `#F7F8FA`; nothing overlaps.
- [ ] If a stat isn't a verified metric, the `footnote` marks it illustrative (honest copy).

### Cover (article hero / featured image — `render_cover.py`, 1600×900)
- [ ] Title is the **exact** article title, fully inside the safe-zone (no clip, no awkward orphan word), crisp Plus Jakarta.
- [ ] Reads **Ahrefs-grade / premium** — NOT generic, NOT "vibe-coded"; clear hierarchy + generous whitespace.
- [ ] On-brand palette; **real logo** present + legible (charcoal on light, white on dark — never AI-drawn); eyebrow/byline correct.
- [ ] Nothing overlaps the title; the motif/illustration is balanced, not cluttered.
- [ ] Survives a centre-crop to ~1.91:1 for OG/social (title + logo stay inside).
- [ ] **Route B (AI background):** the illustration also passes the *Concept illustration* checklist below
      (text-free, on-brand, not slop); the scrim + real logo cover any corner artifact. If unsure → use route A.

### Concept illustration (the gated AI lane — be the STRICTEST critic here; cut on sight)
This is the one type generated by AI, and the one most likely to look cheap. Default to rejecting.
- [ ] Reads **instantly** as the intended concept — the metaphor lands without a caption.
- [ ] Does **NOT** look like generic AI slop: no glossy-3D "app-icon" blob, no clay/plastic, no
      uncanny faces, no melted/garbled geometry, no nonsensical detail, no random stock-art look.
- [ ] **ZERO** text / letters / numbers anywhere (any needed label is composited deterministically,
      never AI-written).
- [ ] On-brand palette — blue `#2E90FA` + violet `#8B5CF6` dominant, mint `#22B276` / coral
      `#FF6B5C` accents only; **not** cyan/teal/pink/random (cross-check `concept_palette_check.py`).
- [ ] ONE clear focal element, tidy, generous negative space — not cluttered.
- [ ] No watermark, signature, or stray marks; nothing cropped/cut off at the frame.
- [ ] **SFW** and tasteful for a public indexed blog (no nudity / suggestive / romantic contact /
      skin-focus — even though the product is adult).
- [ ] Premium **editorial** finish — looks designed, not auto-generated. If in doubt, **CUT it**
      (a missing illustration is fine; a cheap one is not). Recipe: `CONCEPT-ILLUSTRATION-RECIPE.md`.

### Action-shot
- [ ] Shows the **real logged-in product** (NOT a login wall, age gate, blank, or Cloudflare page).
- [ ] **SFW** — clean, aspirational, on-brand; nothing explicit/risqué; explicit image tiles blurred in place.
- [ ] **No private PII** — the user's **email is masked** (`--redact-pii`, default on); username / display name are fine.
- [ ] The intended subject is centred and legible; the meaningful UI isn't cut at an awkward fold.
- [ ] Frame is clean: rounded corners + soft shadow intact; browser URL pill / phone notch correct; not stretched.
- [ ] On-brand canvas + caption + real composited logo; caption matches what's actually shown.
- [ ] Tight crops (`--selector`/`--anchor`) grabbed the right element with even breathing room (no half-cut neighbours).

## Who is the critic
The vision critic is the **agent itself** (it has vision) or a spawned vision sub-agent. The
deterministic report is the cheap first filter; the vision pass catches semantic/aesthetic issues.
Bias toward an extra pass — a wasted render is cheap, a bad published visual is not.
