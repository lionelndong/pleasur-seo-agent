# Reviewer panel — why-ai-companion-forgets

Keyword: "why does my ai companion forget". Three independent skeptical experts,
each having read every page-1 result. Panel passes iff ≥2 KEEP_OURS AND none KEEP_COMPETITOR.

**Panel result: 3 × KEEP_OURS, 0 × KEEP_COMPETITOR, 0 × TOSS_UP → PANEL PASSES.**

All three verdicts exceeded 200 words with specific, non-praise-only critique, so no
sharper re-run was triggered.

---

## Lens A — Competitiveness (depth/specificity/usefulness vs the winners)

VERDICT: KEEP_OURS

This draft genuinely out-explains the incumbents on the one axis the whole SERP hand-waves: it traces a single fact ("Mara grew up on a fishing boat") from typed to evicted across four labeled stages, which is the named information-gain beat that Plurality, Arsturn, DigitalHumanCorp, LLMnesia, and Kenotic all dodge. It hits every consensus topic (tokens, eviction, logs ≠ memory, re-injection layer, bigger-window-isn't-free, context rot) while adding two differentiators none of the page-1 set deliver well — a reader-runnable real-vs-decorative memory test with a Remembered/Partial/Forgot scorecard, and a clean short-term-context-vs-persistent-memory table that beats Kenotic's lone table. The depth is real, not padded; nearly every paragraph earns its place and the BLUF discipline matches LLMnesia's accuracy bar without LLMnesia's dryness.

5 weakest things vs what's ranking:
1. Unfilled citation placeholders (`[link]` ×3 — lines 41, 55, 69) make the draft look LESS sourced than DigitalHumanCorp and LLMnesia on the exact technical claims that establish authority. Biggest competitive liability.
2. "Context rot" (line 51, 55) is asserted then collapsed into "lost in the middle" — conflation looks thinner than Kenotic, the term's owner.
3. Chart at line 61 (`data=research.context_rot_accuracy`) references data the dossier does not establish exists (deep research returned 0 hard citations) — fabrication/render risk; worse than Kenotic's honest static table.
4. Higher-volume sibling phrasings ("does AI remember past conversations," grok/claude/character-ai memory, 400/40/50 vol) get less on-page real estate than on Arsturn/LLMnesia.
5. The "hands-on critical read on apps" info-gain is mostly outsourced to an internal link rather than delivered on-page; "decorative memory" (line 71) is asserted but never demonstrated against a real example.

1 thing that works: The four-stage single-message walkthrough (lines 29–39) — plant → drift → eviction ("Nothing flagged it. No warning fired") → felt forgetting ("what boat?") — is a decisive competitive win no incumbent attempts.

---

## Lens B — Voice & readability (vs the brand voice anchors)

VERDICT: KEEP_OURS

This draft does the one thing the entire SERP refuses to do — it traces a single fact ("Mara grew up on a fishing boat") from typed to evicted, turn by turn, in plain language, which is exactly the information gain the BEAT SPEC demands and the AI Overview cannot replicate. The voice is reader-felt and concrete in the brand's register: it leads with the real decision ("test it, then fix it"), names the gut-punch a roleplayer actually feels, and holds Pleasur.AI to the same critical lens ("a bounded one... no 'never forgets'") instead of pitching. The incumbents (DR 3.6–41) hand-wave the mechanism and ignore the adult-companion angle, so a side-by-side loss is not credible.

5 weakest things vs the voice anchors:
1. Third intro paragraph ("This article walks through five things: ...") is a limp table-of-contents / AI-roadmap tic the anchors avoid. Trim it.
2. Repetition is unmanaged ("window" stacking; "Mara"/fishing-boat reused across three sections feels like the draft has only one example). Matches the floors repetition flags.
3. Bare `[link]` placeholders ×3 — anchors are wall-to-wall live citations (Harvard, Mozilla, Cybernews); a byline blog won't run "[link]".
4. The `[GAIN]` internal pipeline marker is leaking into a reader-facing H2 (line 29) — screams machine-generated if it ships.
5. Forced/stock transitions: "Here's the part that trips people up," "Here's the part no page seems to show you," "the whole game," "gut-punch" — anchors transition on substance, not teasers.

1 thing that works: The turn-by-turn trace (Turn 1 / Turns 2–40 / boundary / next reply) is excellent — concrete, dramatized without being purple, lands adult-companion stakes ("she asks 'what boat?'"). Clearest execution of the mechanism on any page for this query.

---

## Lens C — Reader intent & information gain (vs SERP and AI Overview)

VERDICT: KEEP_OURS

This draft does the one thing the entire SERP and the AI Overview refuse to do: it traces a single concrete fact across four labeled stages from typed → drifting → evicted → "what boat?", the precise information-gain the dossier demands and which no page-1 result delivers. It also ships a genuinely reader-runnable test (state "oxblood"/"Mara," wait 30+ turns or start a new session a day later, score Remembered/Partial/Forgot) and the adult/roleplay framing every generic "AI assistant" page ignores. The AIO can synthesize "context window + tokens + 4 tips" but cannot absorb the staged eviction walkthrough or the scorecard protocol without quoting this article nearly verbatim — exactly the click reason the dossier said survives.

5 weakest things vs what's ranking / the AIO:
1. Unfilled `[link]` placeholders on load-bearing claims ("lost in the middle" lines 41, 55; Character.AI line 69) — LLMnesia/Kenotic already cite; the AIO will cite a real arXiv link. Weaker on the exact axis it's trying to win.
2. Chart (line 61, `research.context_rot_accuracy`) references a dataset that doesn't exist in the dossier; fabrication risk — the SERP's one real table beats a fake chart.
3. "Context rot" is asserted, not taught — the H2 promises a definition but line 51 just says "people call it 'context rot'" and folds it into "lost in the middle." Parity with Kenotic, not a deeper win.
4. Heavy self-reference to two unverified Pleasur.ai blog URLs (lines 39, 80, 88, 106); repeatedly deferring to a "full protocol" elsewhere reads like the payoff is gated, and risks broken-promise pattern if those pages don't exist.
5. Token explanation slightly loose (line 15) and the draft gives no single concrete number anywhere (no example window size, no "~X messages"), weakening the answer to "how many messages does it remember?" vs LLMnesia's accuracy bar.

1 thing that works: The four-stage single-message trace (lines 33–39) is the article's reason to exist. "Nothing flagged it. No warning fired... still sitting in your transcript, invisible to the model" converts the abstract concept every competitor hand-waves into something a non-technical roleplayer feels, and resolves both halves of the query. The AIO cannot reproduce it.
