# Cluster proposals — the growth feed

> Generated by `/cluster-planner` (Layer 6), run 2026-06-30, from `keyword-queue.csv` (34 rows) + `brand-config.md` Products. Never auto-edits `clusters.md` — proposals are for human/EO review.

## No new clusters proposed.

Both discovery sources were checked and neither fired this run:

**(a) Uncovered demand (unclustered themes).** 0 unclustered rows — every queued keyword mapped to an existing active cluster. No theme of ≥ 3 winnable (winnability ≥ 5), BV ≥ 2 members exists outside the current cluster set. (In fact the whole queue holds only 2 winnable rows total — `nsfw image to video` win 10.0 and `anime ai roleplay` win 6.3 — and both already sit inside active clusters as keystones.)

**(b) Uncovered live products.** Every `live` product in `brand-config.md` already maps to an active cluster:

| live product | mapped cluster | status |
|---|---|---|
| AI Companion Creator | companions | covered |
| AI Image Generation | image-gen | covered |
| Voice Replies (in-chat) | voice-calls | covered (cluster active but empty — a seeding gap, not a missing cluster) |
| Phone Call (in-chat) | voice-calls | covered (same) |
| AI Video Generation | — | **roadmap — skipped** (can't demo it yet; not a money cluster) |

No live product lacks a cluster → no product-driven proposal.

---
## Standing notes for the operator (not new-cluster proposals)
- **voice-calls is live-but-empty.** Voice Replies + Phone Call went `live` 2026-06-24 yet the cluster has zero queued keywords. This is a Layer 1b **seeding gap**, not a new cluster — the cluster already exists in `clusters.md`. Action: seed `ai girlfriend voice`, `call an ai`, `ai voice chat`, `ai phone call girlfriend`, `ai voice girlfriend` and re-run the keyword pipeline so the live product gets a beachhead.
- **tools-compare is thin** (1 member, 0 supporting, no winnable keystone). Seed more buyer-intent terms (`best ai girlfriend app`, `[X] vs [Y]`, `candy.ai alternative`, `ai girlfriend alternatives`) in Layer 1b.
- **AI Video Generation** is on `roadmap`. When it flips to `live`, propose a `video-gen` cluster (parent likely `ai porn video generator` / `ai sex video generator`) — and note `nsfw image to video` (current image-gen keystone) would likely migrate there.
