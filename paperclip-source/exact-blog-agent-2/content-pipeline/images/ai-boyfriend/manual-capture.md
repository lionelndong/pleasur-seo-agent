# Manual capture — ai-boyfriend

No manual captures pending. The single comparison visual was rendered deterministically as a matplotlib table-card (no Chrome MCP / generative model required). See manifest.json.
